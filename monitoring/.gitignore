# Nuxt
.nuxt
.output
.data
.cache
dist

# Node
node_modules

# Env
.env
.env.*
!.env.example

# Android native project (committed; these are just its build artifacts)
android/local.properties
android/.gradle
android/captures
android/.externalNativeBuild
android/.cxx
android/*.iml
android/app/build
android/app/release
android/build

# iOS native project (committed; these are just its build artifacts)
ios/App/Pods
ios/App/build
ios/App/App.xcworkspace
ios/.build
